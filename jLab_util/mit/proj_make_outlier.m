function status = proj_make_outlier(ui_struct)
% should move one or more trials from trial status to outlier status
% ui_struct has handle "pointers" to the two lists
% first check if there are trials to be moved
% move them
% mark as dirty, put it in ui_struct?